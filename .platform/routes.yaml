"https://{default}":
  type: upstream
  upstream: "main:http"
  id: "main"

"https://hook.{default}":
  type: upstream
  upstream: "webhook:http"
  id: "webhook"

"https://www.{default}":
  type: redirect
  to: "https://{default}"
